{
  "name": "arctura-ui",
  "packageManager": "yarn@4.11.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "yarn workspaces foreach --all --topological-dev run dev",
    "dev:atomics": "yarn workspace @arctura-ui/atomics dev"
  }
}
